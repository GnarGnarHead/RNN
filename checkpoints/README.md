# Local checkpoints

This folder holds **local** `.pt` checkpoints produced by `session.py` / `scripts/tutor_stepper.py`.

- `.pt`/`.pth`/etc are ignored by git (see `.gitignore`).
- Use this folder as a scratchpad for milestones, for example A-E mimicry,
  A-G all tasks, and A-H all tasks.

Current best known local milestone (see `PROGRESS.md`):
`checkpoints/kinder_ABCDEFGH_alltasks_ctxn_v3.pt`
