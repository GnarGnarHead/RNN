# Local checkpoints

This folder holds **local** `.pt` checkpoints produced by `session.py` / `scripts/tutor_stepper.py`.

- `.pt`/`.pth`/etc are ignored by git (see `.gitignore`).
- Use this folder as a scratchpad for “milestones” (A→E, A→G+tasks, …).

Current “best known” (see `PROGRESS.md`): `checkpoints/kinder_ABCDEFG_alltasks_ctxn_v2.pt`

