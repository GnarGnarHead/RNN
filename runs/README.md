# Local run artifacts

This folder is for **local-only** logs/samples produced by experiments (e.g. `settle_rnn_charlm.py --out-dir runs`).

- Git ignores everything under `runs/` except this file and `.gitkeep`.
- Keep large logs/samples out of git.

